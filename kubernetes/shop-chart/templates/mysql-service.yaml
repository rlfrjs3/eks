apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.mysql.service.name }}

spec:
  clusterIP: None

  selector:
    app: shop-mysql

  ports:
    - name: mysql
      port: {{ .Values.mysql.service.port }}
      targetPort: 3306
      protocol: TCP
